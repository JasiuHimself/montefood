require "rails_helper"

# RSpec.describe Api::MealsController, :type => :controller do
#   describe "GET index (meals)" do
#     it "gets meals for given order and responds with json" do
#       GET :index, params:  1
#       # expect(response).to be_success
#       # # parsed_responce = JSON.parse(response.body)
#       # expect(parsed_responce["status"]).to eq(json[:order][:status])
#       # expect(response.status).to eq(200)
#     end
#   end
# end
